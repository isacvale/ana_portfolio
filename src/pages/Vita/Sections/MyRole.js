import React from "react";
import SectionGrid from "../../../core/Project/SectionGrid";
import SectionTitle from "../../../core/Project/SectionTitle";
import SectionText from "../../../core/Project/SectionText";
import Paragraph from "../../../core/Paragraph";

const MyRole = () => (
  <SectionGrid>
    <SectionTitle>My role</SectionTitle>
    <SectionText>
      <Paragraph>
        In this group project, I worked on personas, competitors research, user
        flow, wireframes, icons, high-fidelity mockups, and QA.
      </Paragraph>
      <Paragraph>
        I also created a study of some elements for a Design System that can be
        found below.
      </Paragraph>
    </SectionText>
  </SectionGrid>
);

export default MyRole;
